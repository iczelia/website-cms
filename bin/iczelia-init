#!/usr/bin/perl
# iczelia.net - personal CMS and static site engine.
# Copyright (C) 2026 Kamila Szewczyk
#
# This program is free software: you can redistribute it and/or modify
# it under the terms of the GNU Affero General Public License as
# published by the Free Software Foundation, either version 3 of the
# License, or (at your option) any later version.
#
# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
# GNU Affero General Public License for more details.
#
# You should have received a copy of the GNU Affero General Public License
# along with this program.  If not, see <https://www.gnu.org/licenses/>.

use strict;
use warnings;
use FindBin ();
use lib "$FindBin::RealBin/../lib";
use Getopt::Long   qw(:config bundling no_auto_abbrev);
use File::Path     qw(make_path);
use File::Basename qw(dirname);

use Iczelia::Config;
use Iczelia::DB;
use Iczelia::Migrate;

my %opt = (
  config => '/etc/iczelia/iczelia.conf',
  force  => 0,
);
GetOptions(
  'config=s' => \$opt{config},
  'force'    => \$opt{force},
  'help|h'   => \&_usage,
) or _usage();

sub _usage {
  print STDERR <<'USAGE';
iczelia-init - create or update the iczelia.net SQLite database.

  --config <path>  config file
  --force          recreate from scratch (DESTROYS existing data)
USAGE
  exit 1;
}

my $cfg = Iczelia::Config->load(\%opt);

my $db_path = $cfg->{db};
if (-e $db_path && $opt{force}) {
  print "wiping existing $db_path\n";
  unlink $db_path or die "unlink $db_path: $!";
}

for my $d ($db_path, $cfg->{'media-dir'}, $cfg->{'tmp-dir'}) {
  next unless $d;
  my $p = $d eq $db_path ? dirname($d) : $d;
  -d $p or make_path($p);
}

my $share = $cfg->{'share-dir'};
my $db    = Iczelia::DB->connect($cfg);

# Migrate FIRST so any ALTER-shape changes (added columns on existing
# tables that CREATE TABLE IF NOT EXISTS won't touch) land before
# schema.sql runs its CREATE INDEX / CREATE TRIGGER statements that
# reference the new columns. Idempotent; a no-op on fresh DBs.
Iczelia::Migrate::run($db);

$db->apply_schema_file("$share/schema.sql");
$db->apply_schema_file("$share/seed.sql");

print "ok: db at $db_path\n";
